apiVersion: v1
kind: Service
metadata:
  name: agent-zero-service
  labels:
    app: agent-zero
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: agent-zero